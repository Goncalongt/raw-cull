{
  "name": "filtered-unmatched-raw-pics",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "author": "Gonçalo Tavares",
  "license": "UNLICENSED",
  "description": "Sort raw picture files and delete those whose matching jpgs were already filtered out",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node scripts/build.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "resedit": "^3.0.0"
  },
  "allowScripts": {
    "esbuild@0.25.12": true
  }
}
